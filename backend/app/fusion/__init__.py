"""Fusion — correlate acoustic + vision cues into unified contacts + alerts.

Placeholder for the engine that fuses an acoustic bearing with a vision
detection into a single confirmed `Contact` (bearing + confidence), and raises
alerts when a cue is confirmed as a drone. Implemented later (see SCOPE.md).
"""
